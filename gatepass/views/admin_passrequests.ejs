<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pass Requests - Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/background.css">
    <link rel="icon" href="/images/4174.png">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container-main {
            background: #fff;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #eee;
        }
        .page-header h1 {
            font-size: 24px;
            color: #1a1a2e;
            margin: 0;
        }
        .nav-links {
            display: flex;
            gap: 10px;
        }
        .nav-links a {
            padding: 8px 16px;
            border-radius: 8px;
            text-decoration: none;
            font-size: 14px;
        }
        .nav-links a.primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
        }
        .nav-links a.secondary {
            background: #f5f5f5;
            color: #333;
        }
        .stats-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 15px;
            margin-bottom: 25px;
        }
        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
        }
        .stat-card.pending { background: linear-gradient(135deg, #f97316 0%, #ea580c 100%); }
        .stat-card.approved { background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%); }
        .stat-card.rejected { background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); }
        .stat-card h3 {
            font-size: 28px;
            margin: 0;
        }
        .stat-card p {
            margin: 5px 0 0;
            opacity: 0.9;
            font-size: 13px;
        }
        .request-card {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 4px solid #667eea;
        }
        .request-card.pending { border-left-color: #f97316; }
        .request-card.approved { border-left-color: #22c55e; }
        .request-card.rejected { border-left-color: #ef4444; }
        .request-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 15px;
        }
        .request-header h4 {
            margin: 0;
            font-size: 18px;
            color: #1a1a2e;
        }
        .request-header .badge {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
        }
        .request-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 15px;
        }
        .request-detail {
            font-size: 14px;
        }
        .request-detail label {
            display: block;
            color: #888;
            font-size: 12px;
            margin-bottom: 3px;
        }
        .request-detail span {
            color: #333;
            font-weight: 500;
        }
        .request-actions {
            display: flex;
            gap: 10px;
            padding-top: 15px;
            border-top: 1px solid #eee;
        }
        .btn-approve {
            background: #22c55e;
            color: #fff;
            border: none;
            padding: 8px 20px;
            border-radius: 8px;
            cursor: pointer;
        }
        .btn-reject {
            background: #ef4444;
            color: #fff;
            border: none;
            padding: 8px 20px;
            border-radius: 8px;
            cursor: pointer;
        }
        .empty-state {
            text-align: center;
            padding: 50px;
            color: #888;
        }
        .empty-state i {
            font-size: 50px;
            margin-bottom: 15px;
        }
        .alert-custom {
            padding: 15px 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        .filter-section {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container-main">
        <div class="page-header">
            <div>
                <h1><i class="fas fa-clipboard-list"></i> Pass Requests</h1>
                <p style="margin: 5px 0 0; color: #888; font-size: 14px;">Manage student pass requests</p>
            </div>
            <div class="nav-links">
                <a href="/daterange" class="secondary"><i class="fas fa-arrow-left"></i> Back</a>
                <a href="/logout" class="secondary"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
        </div>
        
        <% if(message && message.length > 0){ %>
            <div class="alert alert-info alert-custom">
                <i class="fas fa-info-circle"></i> <%- message %>
            </div>
        <% } %>
        
        <!-- Statistics -->
        <div class="stats-cards">
            <div class="stat-card pending">
                <h3><%= stats.pending || 0 %></h3>
                <p>Pending Requests</p>
            </div>
            <div class="stat-card approved">
                <h3><%= stats.approved || 0 %></h3>
                <p>Approved Today</p>
            </div>
            <div class="stat-card rejected">
                <h3><%= stats.rejected || 0 %></h3>
                <p>Rejected Today</p>
            </div>
            <div class="stat-card">
                <h3><%= stats.total || 0 %></h3>
                <p>Total Requests</p>
            </div>
        </div>
        
        <!-- Filter -->
        <div class="filter-section">
            <form method="GET" style="display: flex; gap: 15px; align-items: flex-end; flex-wrap: wrap;">
                <div>
                    <label style="display: block; font-size: 12px; color: #666; margin-bottom: 5px;">Status</label>
                    <select name="status" class="form-select" style="min-width: 150px;">
                        <option value="">All Status</option>
                        <option value="pending" <%= filterStatus === 'pending' ? 'selected' : '' %>>Pending</option>
                        <option value="approved" <%= filterStatus === 'approved' ? 'selected' : '' %>>Approved</option>
                        <option value="rejected" <%= filterStatus === 'rejected' ? 'selected' : '' %>>Rejected</option>
                    </select>
                </div>
                <div>
                    <label style="display: block; font-size: 12px; color: #666; margin-bottom: 5px;">From Date</label>
                    <input type="date" name="fromDate" class="form-control" value="<%= fromDate || '' %>">
                </div>
                <div>
                    <label style="display: block; font-size: 12px; color: #666; margin-bottom: 5px;">To Date</label>
                    <input type="date" name="toDate" class="form-control" value="<%= toDate || '' %>">
                </div>
                <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Filter</button>
                <a href="/admin/passrequests" class="btn btn-secondary">Clear</a>
            </form>
        </div>
        
        <!-- Requests List -->
        <% if(requests && requests.length > 0) { %>
            <% requests.forEach(function(req) { %>
                <div class="request-card <%= req.status %>">
                    <div class="request-header">
                        <div>
                            <h4><%= req.sname %> (<%= req.uid %>)</h4>
                            <small style="color: #888;">Request #<%= req.requestid %> • <%= req.created_at %></small>
                        </div>
                        <span class="badge bg-<%= req.status === 'pending' ? 'warning' : (req.status === 'approved' ? 'success' : 'danger') %>">
                            <%= req.status.charAt(0).toUpperCase() + req.status.slice(1) %>
                        </span>
                    </div>
                    
                    <div class="request-details">
                        <div class="request-detail">
                            <label>Pass Type</label>
                            <span><%= req.passtype %></span>
                        </div>
                        <div class="request-detail">
                            <label>Department</label>
                            <span><%= req.dept || 'N/A' %></span>
                        </div>
                        <div class="request-detail">
                            <label>Expected Out</label>
                            <span><%= req.expected_out %></span>
                        </div>
                        <div class="request-detail">
                            <label>Expected Return</label>
                            <span><%= req.expected_return %></span>
                        </div>
                        <div class="request-detail">
                            <label>Emergency Contact</label>
                            <span><%= req.emergency_contact || 'N/A' %></span>
                        </div>
                        <div class="request-detail">
                            <label>Student Contact</label>
                            <span><%= req.mobileno || 'N/A' %></span>
                        </div>
                    </div>
                    
                    <div class="request-detail" style="margin-bottom: 15px;">
                        <label>Reason/Destination</label>
                        <span><%= req.reason || 'Not specified' %></span>
                    </div>
                    
                    <% if(req.status === 'pending') { %>
                        <div class="request-actions">
                            <form action="/admin/approverequest/<%= req.requestid %>" method="POST" style="display: inline;">
                                <button type="submit" class="btn-approve" onclick="return confirm('Approve this pass request?')">
                                    <i class="fas fa-check"></i> Approve
                                </button>
                            </form>
                            <form action="/admin/rejectrequest/<%= req.requestid %>" method="POST" style="display: inline;">
                                <input type="text" name="reason" placeholder="Rejection reason (optional)" style="padding: 8px; border: 1px solid #ddd; border-radius: 8px; margin-right: 10px;">
                                <button type="submit" class="btn-reject" onclick="return confirm('Reject this pass request?')">
                                    <i class="fas fa-times"></i> Reject
                                </button>
                            </form>
                        </div>
                    <% } else if(req.approved_by) { %>
                        <div style="font-size: 13px; color: #888; padding-top: 10px; border-top: 1px solid #eee;">
                            <strong><%= req.status === 'approved' ? 'Approved' : 'Rejected' %> by:</strong> <%= req.approved_by %> 
                            <% if(req.approved_at) { %>at <%= req.approved_at %><% } %>
                            <% if(req.rejection_reason) { %><br><strong>Reason:</strong> <%= req.rejection_reason %><% } %>
                        </div>
                    <% } %>
                </div>
            <% }); %>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-inbox"></i>
                <h3>No Requests Found</h3>
                <p>There are no pass requests matching your criteria</p>
            </div>
        <% } %>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
